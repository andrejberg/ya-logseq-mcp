---
phase: 11-github-publication-surface
plan: 02
type: execute
wave: 2
depends_on:
  - "11-01"
files_modified:
  - /home/berga/Workspace/tools/ya-logseq-mcp/.gitignore
autonomous: false
requirements:
  - PUB-01

must_haves:
  truths:
    - "A public GitHub repository exists at github.com/andrej-berg/ya-logseq-mcp"
    - "Repository description is 'Python MCP server for Logseq'"
    - "Repository has topics: mcp, logseq, python"
    - "All local commits are pushed to the remote main branch"
  artifacts:
    - path: "github.com/andrej-berg/ya-logseq-mcp"
      provides: "Public GitHub home for the project"
      contains: "ya-logseq-mcp"
  key_links:
    - from: "local main branch"
      to: "github.com/andrej-berg/ya-logseq-mcp"
      via: "git push origin main"
      pattern: "origin.*main"
---

<objective>
Create the public GitHub repository for ya-logseq-mcp, set repository metadata (description and topics), and push the local commit history. The repository becomes publicly visible and discoverable on GitHub.

Purpose: This is the final v1.2 deliverable — the project is published and a GitHub visitor can discover and use it from the repository home.
Output: Public repository at github.com/andrej-berg/ya-logseq-mcp with all local commits and complete metadata.
</objective>

<execution_context>
@/home/berga/.claude/get-shit-done/workflows/execute-plan.md
@/home/berga/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/berga/Workspace/projects/logseq-mcp/.planning/PROJECT.md
@/home/berga/Workspace/projects/logseq-mcp/.planning/ROADMAP.md
@/home/berga/Workspace/projects/logseq-mcp/.planning/STATE.md

<interfaces>
<!-- Repository metadata (locked decisions from CONTEXT.md): -->
<!-- Name: ya-logseq-mcp -->
<!-- Description: "Python MCP server for Logseq" -->
<!-- Topics: mcp, logseq, python -->
<!-- Owner: andrej-berg (personal account) -->
<!-- Visibility: public from initial push -->

<!-- pyproject.toml already has correct GitHub URLs — no changes needed there -->
<!-- No CONTRIBUTING.md, no badges — slim release per Phase 11 decisions -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stage and commit pre-publication changes</name>
  <files>/home/berga/Workspace/tools/ya-logseq-mcp/.gitignore</files>
  <action>
    From /home/berga/Workspace/tools/ya-logseq-mcp:

    Stage and commit all changes produced by Plan 11-01 (gitignore update, LICENSE file, README.md updates, and any git rm --cached deletions from the index):

    ```bash
    cd /home/berga/Workspace/tools/ya-logseq-mcp
    git add .gitignore LICENSE README.md
    git status  # verify only these three files are staged; nothing from .planning/, logseq-test-graph/, CLAUDE.md
    git commit -m "chore: prepare repository for public GitHub publication

    - Exclude .planning/, logseq-test-graph/, CLAUDE.md from tracking
    - Add MIT license (copyright Andrej Berg)
    - Add README pitch and tool table before Requirements section"
    ```

    If `git rm --cached` for .planning/ etc. produced deletions from the index in Plan 11-01, those will appear as staged deletions — include them in the commit by running `git add -u` before the commit. Do NOT use `git add .` (would re-add excluded files).

    Verify the commit contains no .planning/, logseq-test-graph/, or CLAUDE.md files.
  </action>
  <verify>
    <automated>
      cd /home/berga/Workspace/tools/ya-logseq-mcp && git log --oneline -1 && git diff --name-only HEAD~1 HEAD | grep -v '\.planning' | grep -v 'logseq-test-graph' | grep -v 'CLAUDE\.md' | grep -E '(gitignore|LICENSE|README)' && echo "commit OK"
    </automated>
  </verify>
  <done>Single commit on main branch contains .gitignore, LICENSE, README.md changes and no workspace scaffolding files.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify repo contents before public push</name>
  <files>/home/berga/Workspace/tools/ya-logseq-mcp/README.md</files>
  <action>
    PAUSE — human review required before any remote push.

    What was built: Plan 11-01 updated .gitignore, added LICENSE, updated README.md. Task 1 committed those changes to the local main branch.

    Before proceeding to GitHub publication, the user must confirm:
    1. No sensitive or workspace-only files are tracked
    2. README pitch and tool table look correct
    3. LICENSE shows correct copyright holder
  </action>
  <verify>
    <automated>MISSING — this is a human-verify checkpoint. User must run the how-to-verify commands below.</automated>
    <human>
      Run from /home/berga/Workspace/tools/ya-logseq-mcp:

      ```bash
      cd /home/berga/Workspace/tools/ya-logseq-mcp
      git log --oneline -5
      git ls-files | grep -E '\.planning|logseq-test-graph|CLAUDE\.md'  # must be empty
      cat README.md | head -40
      cat LICENSE | head -5
      ```

      Expected:
      - No .planning/, logseq-test-graph/, or CLAUDE.md in tracked files
      - README has pitch and tool table before Requirements
      - LICENSE has "MIT License" and "Andrej Berg"
    </human>
  </verify>
  <done>User has confirmed tracked files are clean, README pitch/tool table are correct, and LICENSE is correct. Approved to push.</done>
  <resume-signal>Type "approved" to proceed with GitHub repo creation and push, or describe any issues to fix first.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create GitHub repository and push</name>
  <files></files>
  <action>
    From /home/berga/Workspace/tools/ya-logseq-mcp, create the public GitHub repository and push:

    ```bash
    cd /home/berga/Workspace/tools/ya-logseq-mcp

    # Create public repo under andrej-berg account
    gh repo create andrej-berg/ya-logseq-mcp \
      --public \
      --description "Python MCP server for Logseq" \
      --source . \
      --remote origin \
      --push
    ```

    If `origin` remote already exists, use:
    ```bash
    git remote add origin https://github.com/andrej-berg/ya-logseq-mcp.git
    git push -u origin main
    ```

    Then set repository topics:
    ```bash
    gh repo edit andrej-berg/ya-logseq-mcp --add-topic mcp --add-topic logseq --add-topic python
    ```

    Verify the repository is visible:
    ```bash
    gh repo view andrej-berg/ya-logseq-mcp
    ```

    Do NOT set homepage URL — not needed for this release.
    Do NOT create releases or tags — out of scope for this plan.
  </action>
  <verify>
    <automated>
      gh repo view andrej-berg/ya-logseq-mcp --json name,description,visibility,repositoryTopics --jq '{name: .name, description: .description, visibility: .visibility, topics: [.repositoryTopics[].topic.name]}'
    </automated>
  </verify>
  <done>
    - Repository exists at github.com/andrej-berg/ya-logseq-mcp
    - Visibility is public
    - Description is "Python MCP server for Logseq"
    - Topics include mcp, logseq, python
    - `git log origin/main` matches local main
  </done>
</task>

</tasks>

<verification>
```bash
# Confirm public repository is live
gh repo view andrej-berg/ya-logseq-mcp

# Confirm no workspace scaffolding in remote
gh api repos/andrej-berg/ya-logseq-mcp/contents/.planning 2>&1 | grep -q "Not Found" && echo "planning dir correctly absent"

# Confirm topics
gh repo view andrej-berg/ya-logseq-mcp --json repositoryTopics --jq '.repositoryTopics[].topic.name'
# Expected: mcp, logseq, python
```
</verification>

<success_criteria>
- Public repository exists at github.com/andrej-berg/ya-logseq-mcp (gh repo view returns without error)
- Repository visibility is public
- Repository description matches "Python MCP server for Logseq" exactly
- Topics: mcp, logseq, python are all present
- Remote main branch has all local commits
- .planning/, logseq-test-graph/, CLAUDE.md are not present in the remote repository
- README.md on GitHub homepage shows pitch, tool table, and existing documentation sections
</success_criteria>

<output>
After completion, create `/home/berga/Workspace/projects/logseq-mcp/.planning/phases/11-github-publication-surface/11-02-SUMMARY.md`
</output>
