---
phase: 04-integration-and-swap
plan: 03
type: execute
wave: 3
depends_on:
  - "04-02"
files_modified:
  - tests/integration/test_graphthulhu_parity.py
  - tests/integration/conftest.py
  - tests/fixtures/graph/README.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - INTG-01
  - INTG-02
  - INTG-03
must_haves:
  truths:
    - "Parity is judged structurally on a fixed fixture page, not by byte-identical JSON payloads"
    - "`logseq-mcp` must prove zero duplicate UUIDs and equal-or-fewer blocks than graphthulhu on the parity fixture page"
    - "The Phase 4 automated evidence is complete before the side-by-side manual smoke validation is attempted"
    - "The shared workspace MCP config can keep both `logseq-mcp` and graphthulhu enabled while the operator validates `logseq-mcp` by server name"
    - "Planning/state docs record the Phase 4 sign-off only after parity evidence and the manual smoke check both pass"
  artifacts:
    - path: "tests/integration/test_graphthulhu_parity.py"
      provides: "Structural parity comparison between graphthulhu and logseq-mcp on the fixture page"
      exports:
        - "test_get_page_parity_fixture_has_fewer_duplicate_blocks_than_graphthulhu"
        - "test_logseq_mcp_preserves_expected_child_nesting_on_parity_fixture"
    - path: "tests/fixtures/graph/README.md"
      provides: "Final operator runbook including parity prerequisites and side-by-side smoke validation steps"
    - path: ".planning/STATE.md"
      provides: "Phase 4 execution state and sign-off updates after parity and manual validation"
    - path: ".planning/ROADMAP.md"
      provides: "Final Phase 4 completion state once integration and manual validation are verified"
  key_links:
    - from: "tests/integration/test_graphthulhu_parity.py"
      to: "tests/fixtures/graph/pages/parity-page.md"
      via: "compares both servers on the fixed parity fixture page"
      pattern: "Parity Fixture"
    - from: "tests/integration/test_graphthulhu_parity.py"
      to: "src/logseq_mcp/tools/core.py"
      via: "parity focuses on the `get_page` deduplication contract"
      pattern: "get_page"
    - from: "tests/fixtures/graph/README.md"
      to: "~/Workspace/.mcp.json"
      via: "documents the shared-config side-by-side validation procedure"
      pattern: "mcp.json"
user_setup:
  - "Keep graphthulhu available alongside `logseq-mcp` in `~/Workspace/.mcp.json` until parity evidence and manual smoke validation both pass"
  - "Optionally export `WORKSPACE_MCP_CONFIG=~/Workspace/.mcp.json` and `GRAPHTHULHU_MCP_SERVER=graphthulhu` before running parity tests outside the default workspace setup"
  - "Perform the final side-by-side smoke test manually on both the isolated graph and the real daily-driver graph"
---

<objective>
Complete parity validation and the side-by-side rollout gate for Phase 4.

Purpose: The final plan closes INTG-03 with reproducible parity evidence and then performs a manual side-by-side smoke validation while both MCP servers remain available in the shared workspace config. Automated tests remain focused on `tests/integration/`; operator validation proves `logseq-mcp` is ready without forcing an immediate graphthulhu removal.

Output: `tests/integration/test_graphthulhu_parity.py`, final fixture runbook updates, and planning-state sign-off once parity plus manual smoke checks pass.
</objective>

<execution_context>
@/home/berga/.claude/get-shit-done/workflows/execute-plan.md
@/home/berga/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-integration-and-swap/04-RESEARCH.md
@.planning/phases/04-integration-and-swap/04-VALIDATION.md
@.planning/phases/04-integration-and-swap/04-02-SUMMARY.md

<interfaces>
Parity assertions must compare:
- page identity
- top-level block ordering
- recursive UUID uniqueness
- expected child nesting on the fixture page
- total block count for `logseq-mcp` less than or equal to graphthulhu

Manual validation gate:
```text
1. Run automated parity tests with graphthulhu still available
2. Confirm ~/Workspace/.mcp.json still contains both the `logseq-mcp` and `graphthulhu` entries
3. Reload the MCP client if needed so both entries are available
4. Run an isolated-graph smoke test explicitly against `logseq-mcp`, including one safe sandbox write
5. Switch back to the real daily-driver graph and run a read-only `logseq-mcp` smoke test against a known note or namespace
6. Keep graphthulhu available as the fallback if any `logseq-mcp` discovery or tool call fails
```

Scope rules:
- Prefer test and planning doc changes only
- Touch server code only if parity evidence exposes a real integration defect
- Do not remove graphthulhu from the shared workspace config before parity and smoke verification are both green

Canonical parity prerequisite:
- `~/Workspace/.mcp.json` contains both `logseq-mcp` and `graphthulhu`
- Parity helpers select the graphthulhu server by name via `GRAPHTHULHU_MCP_SERVER` (default: `graphthulhu`)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add structural parity tests against graphthulhu on the fixed fixture page</name>
  <files>tests/integration/test_graphthulhu_parity.py, tests/integration/conftest.py</files>
  <behavior>
    - `test_get_page_parity_fixture_has_fewer_duplicate_blocks_than_graphthulhu` compares both servers on the same fixture page
    - `logseq-mcp` proves zero duplicate UUIDs anywhere in the returned tree
    - The test asserts equal-or-fewer total blocks than graphthulhu and explains any graphthulhu excess as duplicate UUIDs
    - `test_logseq_mcp_preserves_expected_child_nesting_on_parity_fixture` asserts the known nested structure is intact
  </behavior>
  <action>
    Create `tests/integration/test_graphthulhu_parity.py`:

    1. Add helpers to normalize `get_page` outputs from both servers into a comparable structural form.
    2. Define the graphthulhu invocation contract explicitly in test helpers and docs: parity tests read the shared workspace MCP config and select the `graphthulhu` server entry by name.
    3. Compare responses on the fixed parity fixture page only.
    4. Assert that `logseq-mcp` returns no duplicate UUIDs and preserves the expected child nesting.
    5. Assert that `logseq-mcp` block count is less than or equal to graphthulhu for the fixture page.
    6. Keep the assertions structural. Do not require full payload identity.
  </action>
  <verify>
    <automated>cd /home/berga/Workspace/projects/logseq-mcp && uv run pytest tests/integration/test_graphthulhu_parity.py -x -q -m integration</automated>
  </verify>
  <done>INTG-03 has automated parity evidence on a deterministic fixture page while graphthulhu remains available for rollback</done>
</task>

<task type="manual">
  <name>Task 2: Execute the side-by-side manual smoke gate and record sign-off</name>
  <files>tests/fixtures/graph/README.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <behavior>
    - The runbook documents the exact shared-config validation, smoke-test, and fallback procedure
    - `~/Workspace/.mcp.json` retains both MCP server entries during validation
    - The MCP client can discover and call the `logseq-mcp` tools by name while graphthulhu remains available
    - Planning state is updated only after the isolated-graph smoke test and the daily-driver read-only smoke test both pass
  </behavior>
  <action>
    Finalize the release gate:

    1. Update `tests/fixtures/graph/README.md` with the exact parity prerequisite, shared workspace config contract, manual smoke-test checklist, and fallback guidance.
    2. Confirm `~/Workspace/.mcp.json` still contains both the `logseq-mcp` and `graphthulhu` server entries, then reload the MCP client if needed.
    3. Run a manual smoke test explicitly against `logseq-mcp` on the isolated graph:
       - `health`
       - `get_page` on the parity fixture page
       - `list_pages`
       - one safe write on the sandbox page
    4. Switch Logseq back to the real daily-driver graph and run a read-only `logseq-mcp` smoke test against a known note or namespace:
       - `health`
       - `get_page` on a known daily-driver note
       - `list_pages` on a known namespace or baseline filter already used in daily work
    5. If any step fails, keep using graphthulhu as fallback and record the blocker instead of updating planning state.
    6. If all steps pass, update `.planning/STATE.md` and `.planning/ROADMAP.md` to mark Phase 4 complete.
  </action>
  <verify>
    <automated>cd /home/berga/Workspace/projects/logseq-mcp && uv run pytest tests/integration -v -m integration</automated>
    <manual>Keep both MCP servers configured in `~/Workspace/.mcp.json`, validate `logseq-mcp` by name on the isolated graph, then run a read-only daily-driver smoke test and fall back to graphthulhu if any step fails</manual>
  </verify>
  <done>Parity evidence, manual `logseq-mcp` smoke testing, and planning-state updates are all complete, with graphthulhu retained as fallback in the runbook</done>
</task>

</tasks>

<verification>
Run:
- `uv run pytest tests/integration/test_graphthulhu_parity.py -x -q -m integration`
- `uv run pytest tests/integration -v -m integration`
- `uv run pytest tests/ -v -m "not integration"`

Manual release gate:
- Confirm `~/Workspace/.mcp.json` still contains both `logseq-mcp` and `graphthulhu`
- Export `WORKSPACE_MCP_CONFIG=~/Workspace/.mcp.json` and `GRAPHTHULHU_MCP_SERVER=graphthulhu` only if the defaults are not already correct
- Reload the MCP client if needed
- Run the isolated-graph smoke test against `logseq-mcp` (`health`, `get_page`, `list_pages`, one sandbox write)
- Switch back to the real daily-driver graph and run a read-only `logseq-mcp` smoke test (`health`, `get_page` on a known note, `list_pages` on a known namespace/filter)
- Fall back to graphthulhu immediately if any `logseq-mcp` discovery or tool call fails

Expected after this plan: INTG-01, INTG-02, and INTG-03 are all evidenced, and `logseq-mcp` is validated for side-by-side use while graphthulhu remains available as fallback until a later cleanup decision.
</verification>

<success_criteria>
- Graphthulhu parity is automated and reproducible on the fixed fixture page
- `logseq-mcp` demonstrates the deduplication win without breaking nesting
- The side-by-side validation procedure is documented with a clear fallback path
- The MCP client can use `logseq-mcp` while graphthulhu remains available in the shared config
- Planning state and roadmap are updated only after automated and manual gates both pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-integration-and-swap/04-03-SUMMARY.md`
</output>
