---
phase: 04-integration-and-swap
task: 2
total_tasks: 2
status: in_progress
last_updated: 2026-03-10T21:04:53.443Z
---

<current_state>
Phase 4 Plan 03 is paused after Task 1 completed and Task 2 remains manual. The parity harness now reads the shared workspace MCP config at ~/Workspace/.mcp.json and selects the graphthulhu server by name, so both logseq-mcp and graphthulhu can stay enabled side by side. The plan, roadmap, and fixture runbook were revised to remove the old forced swap model and replace it with a side-by-side validation gate.
</current_state>

<completed_work>

- Task 1: Add structural parity tests against graphthulhu on the fixed fixture page - Done
- Verified that both `logseq-mcp` and `graphthulhu` are reachable by name from `~/Workspace/.mcp.json` - Done
- Updated `tests/integration/conftest.py` so parity tests load the shared workspace config and select `graphthulhu` by `GRAPHTHULHU_MCP_SERVER` (default `graphthulhu`) - Done
- Updated `tests/fixtures/graph/README.md` to document the shared-config parity prerequisite and side-by-side manual validation runbook - Done
- Updated `.planning/phases/04-integration-and-swap/04-03-PLAN.md` to define the manual gate as side-by-side validation instead of config replacement - Done
- Updated `.planning/ROADMAP.md` to align Phase 4 success criteria with side-by-side rollout readiness - Done
- Verified `uv run pytest tests/integration/test_graphthulhu_parity.py -x -q -m integration` passes with the shared workspace config - Done
</completed_work>

<remaining_work>

- Task 2: Execute the side-by-side manual smoke gate
- Confirm `~/Workspace/.mcp.json` still contains both `logseq-mcp` and `graphthulhu`
- Reload the MCP client if needed so both entries are available
- Run an isolated-graph smoke test explicitly against `logseq-mcp`: `health`, `get_page` on `Phase 04 Parity Fixture`, `list_pages`, and one safe write on `Phase 04 Write Sandbox`
- Switch Logseq back to the real daily-driver graph and run a read-only `logseq-mcp` smoke test: `health`, `get_page` on a known note, and `list_pages` on a known namespace/filter
- If the manual gate passes, update `.planning/STATE.md` and `.planning/ROADMAP.md` to mark Phase 4 complete and create `04-03-SUMMARY.md`
- If any `logseq-mcp` discovery or tool call fails, keep using graphthulhu as fallback and record the blocker instead of marking the phase complete
</remaining_work>

<decisions_made>

- Decided not to use `~/.claude/.mcp.json` because it does not exist in this environment; the relevant config is `~/Workspace/.mcp.json`
- Decided to keep both MCP servers in the shared workspace config because the harness can target each server by name deterministically
- Chose explicit server-name selection over single-server config files because it removes unnecessary config churn and makes rollback trivial
- Revised Phase 4 acceptance from “replace graphthulhu” to “validate logseq-mcp side by side” because the shared config already supports both servers concurrently
</decisions_made>

<blockers>
- No code blocker remains
- Remaining work is intentionally manual and operator-driven; planning state should not be updated until those smoke checks are performed
</blockers>

<context>
The important mental model is that Phase 4 is no longer about swapping config files. It is now about proving parity automatically, then proving `logseq-mcp` is safe to use manually while graphthulhu remains available as fallback. The highest-signal files for resumption are the revised plan, the fixture README runbook, the shared workspace MCP config, and the still-stale `.planning/STATE.md`, which has not been updated yet because the manual gate has not happened.
</context>

<next_action>
Start with: perform the manual side-by-side smoke validation described in `tests/fixtures/graph/README.md`, then update `.planning/STATE.md`, `.planning/ROADMAP.md`, and write `04-03-SUMMARY.md` only if those manual checks pass.
</next_action>
