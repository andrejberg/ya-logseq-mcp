---
phase: 04-integration-and-swap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/integration/conftest.py
  - tests/integration/test_live_graph.py
  - tests/fixtures/graph/pages/parity-page.md
  - tests/fixtures/graph/pages/write-sandbox.md
  - tests/fixtures/graph/README.md
  - pyproject.toml
autonomous: true
requirements:
  - INTG-02
must_haves:
  truths:
    - "Integration writes fail fast unless Logseq is pointed at the isolated test graph"
    - "Phase 4 introduces deterministic graph fixtures for parity and write-sandbox validation"
    - "Live-graph tests stay opt-in and do not run in the default fast unit-test loop"
    - "The isolated graph contract is enforced in test helpers before the first mutation RPC"
    - "Fixture docs explain how to open the dedicated test graph and restore sandbox state before every run"
  artifacts:
    - path: "tests/integration/conftest.py"
      provides: "Shared isolated-graph guards, env fixtures, and live-client helpers"
      exports:
        - "isolated_graph_env"
        - "live_client"
        - "assert_isolated_graph"
        - "seed_fixture_graph"
    - path: "tests/integration/test_live_graph.py"
      provides: "Live Logseq integration coverage for graph isolation and safe write scope"
      exports:
        - "test_health_requires_isolated_graph"
        - "test_fixture_pages_are_present_before_mutation"
        - "test_write_tools_use_sandbox_page_only"
    - path: "tests/fixtures/graph/pages/parity-page.md"
      provides: "Deterministic nested page content for Phase 4 parity validation"
    - path: "tests/fixtures/graph/pages/write-sandbox.md"
      provides: "Safe mutation target for live integration writes"
    - path: "tests/fixtures/graph/README.md"
      provides: "Operator instructions for creating and opening the isolated Logseq graph"
  key_links:
    - from: "tests/integration/conftest.py"
      to: "src/logseq_mcp/client.py"
      via: "live integration helpers use the real LogseqClient"
      pattern: "LogseqClient"
    - from: "tests/integration/test_live_graph.py"
      to: "src/logseq_mcp/tools/write.py"
      via: "live write verification exercises the existing write-tool contract"
      pattern: "page_create|block_append|block_update|block_delete"
    - from: "tests/fixtures/graph/README.md"
      to: "tests/fixtures/graph/pages/parity-page.md"
      via: "fixture setup documents the canonical parity page"
      pattern: "Parity Fixture"
user_setup:
  - "Create or open a dedicated Logseq graph for Phase 4, separate from the daily-driver workspace graph"
  - "Export LOGSEQ_API_TOKEN for the running Logseq app and ensure the isolated graph is the active graph before live tests"
---

<objective>
Create the isolated integration harness for Phase 4.

Purpose: INTG-02 needs hard safety rails before any stdio or parity work starts. This plan establishes the dedicated graph fixtures, explicit graph-name guards, and opt-in live tests so Phase 4 evidence can be collected without touching the real workspace graph.

Output: `tests/integration/conftest.py`, `tests/integration/test_live_graph.py`, and fixture graph docs/content under `tests/fixtures/graph/`.
</objective>

<execution_context>
@/home/berga/.claude/get-shit-done/workflows/execute-plan.md
@/home/berga/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-integration-and-swap/04-RESEARCH.md
@.planning/phases/04-integration-and-swap/04-VALIDATION.md

<interfaces>
Expected test split for this plan:
```text
tests/
├── integration/
│   ├── conftest.py
│   └── test_live_graph.py
└── fixtures/
    └── graph/
        ├── pages/
        │   ├── parity-page.md
        │   └── write-sandbox.md
        └── README.md
```

Scope rules:
- Keep the work in `tests/` and `pyproject.toml`
- Add a pytest marker for opt-in integration runs if one does not already exist
- Do not modify server code in this plan unless the harness proves a real integration defect

Isolation contract:
- Tests must assert the active graph name before writes
- The parity fixture page and write sandbox page must be deterministic
- The write sandbox must be reset or namespaced before each run so repeated executions start from known state
- Live integration tests must be explicit, not part of the default `uv run pytest tests/ -v` loop
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build isolated graph fixtures and opt-in integration test plumbing</name>
  <files>tests/integration/conftest.py, tests/fixtures/graph/pages/parity-page.md, tests/fixtures/graph/pages/write-sandbox.md, tests/fixtures/graph/README.md, pyproject.toml</files>
  <behavior>
    - `tests/integration/conftest.py` provides env and graph-name guard helpers for live tests
    - Integration tests are protected by an explicit pytest marker or equivalent opt-in configuration
    - Fixture graph docs describe how to create or reseed the dedicated Logseq test graph
    - The parity fixture page contains nested blocks deep enough to expose duplicate traversal bugs
    - The write sandbox page is the only sanctioned target for destructive live mutations
  </behavior>
  <action>
    Create the Phase 4 integration harness foundation:

    1. Add `tests/integration/conftest.py` with helpers to:
       - read required env such as `LOGSEQ_API_TOKEN`
       - create a real `LogseqClient`
       - assert the active graph name matches the isolated fixture graph before writes
       - reseed or verify the presence of the fixture pages
       - reset the sandbox page to a known baseline, or create a per-run namespaced sandbox target before destructive tests
    2. Add `tests/fixtures/graph/pages/parity-page.md` with fixed nested content named for the parity comparison.
    3. Add `tests/fixtures/graph/pages/write-sandbox.md` with a reserved sandbox page for append, update, and delete tests.
    4. Add `tests/fixtures/graph/README.md` documenting:
       - the required graph name
       - how to copy the fixture pages into a disposable graph
       - the command sequence for opening the graph in Logseq before tests
       - the rule that no live write test may run against the daily-driver graph
    5. Update `pyproject.toml` with a concrete integration marker if needed so live tests remain opt-in.
  </action>
  <verify>
    <automated>cd /home/berga/Workspace/projects/logseq-mcp && uv run pytest tests/integration/test_live_graph.py::test_health_requires_isolated_graph -x -q</automated>
  </verify>
  <done>Fixture docs and conftest helpers exist, and the isolated-graph preflight test can fail safely before any mutation when the wrong graph is open</done>
</task>

<task type="auto">
  <name>Task 2: Add live graph safety and sandbox verification tests</name>
  <files>tests/integration/test_live_graph.py, tests/integration/conftest.py</files>
  <behavior>
    - `test_health_requires_isolated_graph` fails fast when Logseq reports the wrong graph
    - `test_fixture_pages_are_present_before_mutation` verifies the parity and sandbox pages are reachable before writes
    - `test_write_tools_use_sandbox_page_only` performs safe live mutations only against the sandbox page and verifies readback
    - The live tests prove INTG-02 without widening scope into stdio or graphthulhu parity yet
  </behavior>
  <action>
    Create `tests/integration/test_live_graph.py` with focused live-graph coverage:

    1. Use the real client and existing tool contract to verify `health` reports the expected isolated graph.
    2. Add a preflight test that checks both fixture page names exist before any live write occurs.
    3. Add a sandbox-only write test that exercises one safe append/update/delete flow against the reserved page and confirms the resulting page tree is correct on follow-up reads.
    4. Reset the sandbox page before each destructive run, or use a deterministic per-run child namespace so the test remains repeatable across executions.
    5. Keep assertions structural and deterministic. Do not reuse page names from the daily workflow.
    6. Make failures explicit when env is missing, the graph name is wrong, or fixture pages are absent.
  </action>
  <verify>
    <automated>cd /home/berga/Workspace/projects/logseq-mcp && uv run pytest tests/integration/test_live_graph.py -x -q -m integration</automated>
  </verify>
  <done>`tests/integration/test_live_graph.py` proves graph isolation and sandbox-only writes against the dedicated test graph</done>
</task>

</tasks>

<verification>
Run:
- `uv run pytest tests/integration/test_live_graph.py::test_health_requires_isolated_graph -x -q -m integration`
- `uv run pytest tests/integration/test_live_graph.py::test_fixture_pages_are_present_before_mutation -x -q -m integration`
- `uv run pytest tests/integration/test_live_graph.py::test_write_tools_use_sandbox_page_only -x -q -m integration`

Expected after this plan: INTG-02 has concrete fixture docs, explicit isolated-graph guards, and live tests that mutate only the sandbox page. No stdio transport or graphthulhu parity assertions are required yet.
</verification>

<success_criteria>
- `tests/integration/` exists with reusable live-test helpers
- Fixture pages and setup docs are committed under `tests/fixtures/graph/`
- Integration tests are explicit and opt-in
- Live write verification fails fast if the wrong graph is active
- Repeated live runs restore or isolate sandbox state before mutating the graph
- Phase 4 can proceed without risking the real workspace graph
</success_criteria>

<output>
After completion, create `.planning/phases/04-integration-and-swap/04-01-SUMMARY.md`
</output>
