---
phase: 04-integration-and-swap
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - tests/integration/conftest.py
  - tests/integration/test_mcp_stdio.py
  - tests/integration/test_live_graph.py
autonomous: true
requirements:
  - INTG-01
  - INTG-02
must_haves:
  truths:
    - "`uv run python -m logseq_mcp` is verified as a real MCP stdio server, not just an importable module"
    - "The stdio harness initializes MCP, lists tools, and invokes both read and write tools through transport"
    - "stdout carries protocol traffic only; logging remains on stderr"
    - "The stdio tests reuse the isolated graph guards from Plan 01 before any write call"
    - "Environment propagation is proven from subprocess launch to live Logseq API access"
  artifacts:
    - path: "tests/integration/test_mcp_stdio.py"
      provides: "Black-box MCP stdio transport coverage for server startup, tool discovery, and tool calls"
      exports:
        - "test_stdio_server_exposes_expected_tools"
        - "test_mcp_health_and_get_page_round_trip"
        - "test_mcp_write_round_trip_uses_isolated_graph"
        - "test_server_keeps_logs_off_stdout"
    - path: "tests/integration/conftest.py"
      provides: "Shared subprocess launch and MCP session helpers for stdio integration"
      exports:
        - "launch_stdio_server"
        - "mcp_session"
        - "assert_protocol_clean_stdout"
  key_links:
    - from: "tests/integration/test_mcp_stdio.py"
      to: "src/logseq_mcp/__main__.py"
      via: "launches the production entrypoint exactly as Claude will"
      pattern: "python -m logseq_mcp"
    - from: "tests/integration/test_mcp_stdio.py"
      to: "src/logseq_mcp/server.py"
      via: "tool discovery must reflect the registered FastMCP tool set"
      pattern: "FastMCP|mcp"
    - from: "tests/integration/test_mcp_stdio.py"
      to: "tests/integration/test_live_graph.py"
      via: "stdio tests reuse isolated graph assumptions established in Plan 01"
      pattern: "isolated graph"
user_setup:
  - "Keep the isolated Phase 4 test graph open in Logseq while stdio tests run"
  - "Ensure the same shell environment used for `uv run pytest` includes the valid `LOGSEQ_API_TOKEN`"
---

<objective>
Verify the production stdio MCP entrypoint end to end.

Purpose: INTG-01 is not satisfied by internal registry tests. This plan launches `python -m logseq_mcp` as a subprocess, speaks MCP over stdio, and proves Claude-facing transport behavior using the isolated graph from Plan 01.

Output: `tests/integration/test_mcp_stdio.py` plus any shared helper updates needed for a reusable stdio test harness.
</objective>

<execution_context>
@/home/berga/.claude/get-shit-done/workflows/execute-plan.md
@/home/berga/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-integration-and-swap/04-RESEARCH.md
@.planning/phases/04-integration-and-swap/04-VALIDATION.md
@.planning/phases/04-integration-and-swap/04-01-SUMMARY.md

<interfaces>
Required transport checks:
```text
1. Launch `uv run python -m logseq_mcp`
2. Initialize an MCP session over stdio
3. List registered tools
4. Call `health`
5. Call `get_page` on the parity fixture page
6. Call one safe write tool against the sandbox page
7. Assert protocol traffic stays on stdout and logs stay on stderr
```

Tool list must include at least:
- `health`
- `get_page`
- `get_block`
- `list_pages`
- `get_references`
- `page_create`
- `block_append`
- `block_update`
- `block_delete`

Scope rules:
- Keep this plan in `tests/integration/` unless the stdio harness exposes a real server defect
- Reuse the production entrypoint; do not add a test-only launcher
- Do not begin graphthulhu parity in this plan
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add reusable subprocess and MCP session helpers for stdio integration</name>
  <files>tests/integration/conftest.py</files>
  <behavior>
    - Shared helpers launch `uv run python -m logseq_mcp` with the required env
    - The harness can initialize MCP over stdio and terminate the process cleanly
    - Helpers expose stdout/stderr capture so protocol contamination can be asserted explicitly
    - The isolated graph guard from Plan 01 is reused before any write through transport
  </behavior>
  <action>
    Extend `tests/integration/conftest.py` with stdio-level helpers:

    1. Add a subprocess launcher for the production module entrypoint using `uv run python -m logseq_mcp`.
    2. Add MCP client/session utilities from the installed `mcp` package or equivalent project-supported helpers to drive initialize, list tools, and call tools over stdio.
    3. Ensure the helper passes through `LOGSEQ_API_TOKEN` and any required URL overrides.
    4. Capture stdout and stderr separately so tests can assert protocol cleanliness.
    5. Shut down the subprocess deterministically even on assertion failure.
  </action>
  <verify>
    <automated>cd /home/berga/Workspace/projects/logseq-mcp && uv run pytest tests/integration/test_mcp_stdio.py::test_stdio_server_exposes_expected_tools -x -q -m integration</automated>
  </verify>
  <done>Reusable stdio helpers exist and can launch, initialize, and stop the production MCP server subprocess</done>
</task>

<task type="auto">
  <name>Task 2: Add black-box stdio transport tests for startup, tool calls, and stdout hygiene</name>
  <files>tests/integration/test_mcp_stdio.py, tests/integration/conftest.py, tests/integration/test_live_graph.py</files>
  <behavior>
    - `test_stdio_server_exposes_expected_tools` proves the subprocess launches and registers the required tool set
    - `test_mcp_health_and_get_page_round_trip` proves MCP round trips for `health` and `get_page` on the parity fixture page
    - `test_mcp_write_round_trip_uses_isolated_graph` proves one safe write succeeds only when the isolated graph is active
    - `test_server_keeps_logs_off_stdout` detects stdout contamination outside MCP protocol frames
  </behavior>
  <action>
    Create `tests/integration/test_mcp_stdio.py` with black-box MCP transport coverage:

    1. Initialize a subprocess-backed MCP session and assert the expected tool names are returned from `tools/list`.
    2. Call `health` and assert the graph name matches the isolated test graph.
    3. Call `get_page` on the parity fixture page and assert the response is structurally valid.
    4. Call one safe write tool against the sandbox page and confirm follow-up readback through MCP, not direct Python imports.
    5. Add explicit assertions that any non-protocol logging remains on stderr rather than polluting stdout.
    6. Keep tests deterministic and independent of graphthulhu.
  </action>
  <verify>
    <automated>cd /home/berga/Workspace/projects/logseq-mcp && uv run pytest tests/integration/test_mcp_stdio.py -x -q -m integration</automated>
  </verify>
  <done>The production stdio entrypoint is verified end to end for initialization, tool discovery, read/write tool calls, and stdout/stderr hygiene</done>
</task>

</tasks>

<verification>
Run:
- `uv run pytest tests/integration/test_mcp_stdio.py::test_stdio_server_exposes_expected_tools -x -q -m integration`
- `uv run pytest tests/integration/test_mcp_stdio.py::test_mcp_health_and_get_page_round_trip -x -q -m integration`
- `uv run pytest tests/integration/test_mcp_stdio.py::test_mcp_write_round_trip_uses_isolated_graph -x -q -m integration`
- `uv run pytest tests/integration/test_mcp_stdio.py::test_server_keeps_logs_off_stdout -x -q -m integration`
- `uv run pytest tests/integration/test_live_graph.py tests/integration/test_mcp_stdio.py -x -q -m integration`

Expected after this plan: INTG-01 is covered by a real stdio subprocess harness, and INTG-02 remains enforced through the isolated-graph helpers.
</verification>

<success_criteria>
- `python -m logseq_mcp` is tested as a black-box MCP stdio server
- Required tools are discoverable through MCP transport
- At least one read flow and one safe write flow pass through MCP stdio
- stdout remains protocol-clean
- The isolated graph contract from Plan 01 is preserved for all write calls
</success_criteria>

<output>
After completion, create `.planning/phases/04-integration-and-swap/04-02-SUMMARY.md`
</output>
