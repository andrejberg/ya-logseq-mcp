---
phase: 03-write-tools
plan: 03
type: execute
wave: 3
depends_on:
  - "03-02"
files_modified:
  - src/logseq_mcp/server.py
  - src/logseq_mcp/tools/write.py
  - tests/test_server.py
  - tests/test_write.py
autonomous: true
requirements:
  - WRIT-04
  - WRIT-05
user_setup: []

must_haves:
  truths:
    - "`block_update` updates block content by UUID and confirms the change via a follow-up read"
    - "`block_delete` removes a block by UUID and confirms absence via a follow-up read"
    - "The write module is registered from `server.py` so Phase 3 tools are available through MCP"
    - "Write-tool error paths raise explicit failures for missing targets or malformed RPC responses"
    - "Phase 3 test suite and server registration checks pass without adding v2 write scope"
  artifacts:
    - path: "src/logseq_mcp/tools/write.py"
      provides: "block_update and block_delete with read-after-write verification"
      exports:
        - "block_update"
        - "block_delete"
        - "_verify_block_readback"
        - "_verify_block_absent"
    - path: "src/logseq_mcp/server.py"
      provides: "Write module registration on the FastMCP server"
    - path: "tests/test_write.py"
      provides: "Green tests for WRIT-04 and WRIT-05"
    - path: "tests/test_server.py"
      provides: "Registration or smoke coverage for the new write module"
  key_links:
    - from: "src/logseq_mcp/server.py"
      to: "src/logseq_mcp/tools/write.py"
      via: "bottom-of-file import for tool registration"
      pattern: "from logseq_mcp.tools import write as _write"
    - from: "src/logseq_mcp/tools/write.py"
      to: "src/logseq_mcp/tools/core.py"
      via: "follow-up reads use existing Phase 2 data shapes"
      pattern: "getBlock|getPageBlocksTree"
---

<objective>
Complete Phase 3 by implementing `block_update`, `block_delete`, and registering the write module with the MCP server.

Purpose: This closes the remaining write requirements and makes the full Phase 3 write surface executable through the server, while preserving the same read-after-write guarantees established earlier in the phase.

Output: `block_update`, `block_delete`, write-module registration in `server.py`, and passing tests for WRIT-04 and WRIT-05 plus registration coverage.
</objective>

<execution_context>
@/home/berga/.claude/get-shit-done/workflows/execute-plan.md
@/home/berga/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-write-tools/03-RESEARCH.md
@.planning/phases/03-write-tools/03-VALIDATION.md
@.planning/phases/03-write-tools/03-02-SUMMARY.md

<interfaces>
Primary RPCs for this plan:
```python
await client._call("logseq.Editor.updateBlock", uuid, content)
await client._call("logseq.Editor.removeBlock", uuid)
await client._call("logseq.Editor.getBlock", uuid, {"includeChildren": True})
await client._call("logseq.Editor.getPageBlocksTree", page_name)
```

Registration pattern from previous phases:
```python
mcp = FastMCP("logseq-mcp", lifespan=lifespan)
from logseq_mcp.tools import core as _core  # noqa: E402, F401
from logseq_mcp.tools import write as _write  # noqa: E402, F401
```

Scope guard:
- Do not add `delete_page`, `rename_page`, or `move_block`
- Do not widen test scope beyond Phase 3 write requirements and registration
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement block_update and block_delete with read-after-write verification</name>
  <files>src/logseq_mcp/tools/write.py, tests/test_write.py</files>
  <behavior>
    - `block_update` updates a block by UUID and verifies the new content through a follow-up read
    - `block_delete` removes a block by UUID and verifies that later reads no longer surface the block
    - Missing UUIDs raise explicit errors instead of returning silent success
    - Tool responses remain JSON strings
  </behavior>
  <action>
    Extend `src/logseq_mcp/tools/write.py`:

    1. Add `_verify_block_readback(...)` to confirm updated content after `updateBlock`.
    2. Add `_verify_block_absent(...)` to confirm absence after `removeBlock`.
    3. Implement `block_update(ctx: Context, uuid: str, content: str) -> str`:
       - update via `logseq.Editor.updateBlock`
       - verify with `getBlock` or page-tree readback
       - raise explicit error on `None`, malformed responses, or unchanged content
    4. Implement `block_delete(ctx: Context, uuid: str) -> str`:
       - remove via `logseq.Editor.removeBlock`
       - preflight `getBlock(uuid, {"includeChildren": True})` to capture the containing page before deletion
       - verify block absence on follow-up `getBlock`
       - if page context is available from preflight, verify the captured page tree no longer contains the UUID
       - raise explicit error on missing block or unexpected follow-up state

    Update `tests/test_write.py` so WRIT-04 and WRIT-05 assertions go GREEN, including explicit missing-UUID negative-path tests.
  </action>
  <verify>
    <automated>cd /home/berga/Workspace/projects/logseq-mcp && uv run pytest tests/test_write.py::test_block_update_changes_content_and_verifies_readback tests/test_write.py::test_block_delete_removes_block_from_followup_reads -x -q</automated>
  </verify>
  <done>`block_update` and `block_delete` are implemented with read-after-write verification and the WRIT-04/05 plus missing-UUID tests pass GREEN</done>
</task>

<task type="auto">
  <name>Task 2: Register the write module on the MCP server and validate Phase 3 coverage</name>
  <files>src/logseq_mcp/server.py, tests/test_server.py, tests/test_write.py</files>
  <behavior>
    - `server.py` imports the write module so `page_create`, `block_append`, `block_update`, and `block_delete` register on `mcp`
    - Existing server tests continue to pass
    - Registration or smoke tests confirm write tools are present
    - Full Phase 3 test file passes
  </behavior>
  <action>
    1. Update `src/logseq_mcp/server.py` to import the write module after `mcp` is created, matching the existing bottom-of-file registration pattern.
    2. Extend `tests/test_server.py` or add targeted assertions so the registered tool list includes the Phase 3 write tools.
    3. Clean up `tests/test_write.py` if needed so the full file reflects final GREEN-state behavior only.
    4. Keep scope strictly to registration and Phase 3 validation. Do not add extra write tools from `CLAUDE.md`.
  </action>
  <verify>
    <automated>cd /home/berga/Workspace/projects/logseq-mcp && uv run pytest tests/test_server.py -q</automated>
  </verify>
  <done>`server.py` registers the write module, server coverage passes, and the full Phase 3 test suite is ready for execute-phase consumption</done>
</task>

</tasks>

<verification>
Run:
- `uv run pytest tests/test_write.py::test_block_update_changes_content_and_verifies_readback tests/test_write.py::test_block_delete_removes_block_from_followup_reads -x -q`
- `uv run pytest tests/test_write.py::test_block_update_missing_uuid_raises_explicit_error tests/test_write.py::test_block_delete_missing_uuid_raises_explicit_error -x -q`
- `uv run pytest tests/test_server.py -q`
- `uv run pytest tests/test_write.py -x -q`
- `uv run pytest tests/ -v`

Manual verification before phase sign-off:
- Confirm the earlier Plan 02 page-property UI checkpoint already passed on a disposable page
- Append a nested payload to a disposable page and confirm sibling order plus parent/child placement visually in Logseq
</verification>

<success_criteria>
- `block_update` and `block_delete` satisfy WRIT-04 and WRIT-05 with follow-up verification
- `server.py` registers all four Phase 3 write tools
- `tests/test_write.py` passes end-to-end
- `tests/test_server.py` confirms write-tool registration without regressions
- Full test suite passes and no v2 write scope was added
</success_criteria>

<output>
After completion, create `.planning/phases/03-write-tools/03-03-SUMMARY.md`
</output>
