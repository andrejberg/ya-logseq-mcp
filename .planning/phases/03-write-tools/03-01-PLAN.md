---
phase: 03-write-tools
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_write.py
autonomous: true
requirements:
  - WRIT-01
  - WRIT-02
  - WRIT-03
  - WRIT-04
  - WRIT-05
user_setup: []

must_haves:
  truths:
    - "tests/test_write.py exists and pytest collects all Phase 3 write-tool tests"
    - "Write-tool tests express read-after-write expectations using existing Phase 2 read patterns"
    - "The red-state run fails due to missing write-tool implementation, not import or syntax errors"
    - "At least one test covers page properties, one covers nested append hierarchy, one covers update readback, and one covers delete absence"
  artifacts:
    - path: "tests/test_write.py"
      provides: "Failing test scaffold for WRIT-01 through WRIT-05"
      exports:
        - "test_page_create_with_properties_and_initial_blocks"
        - "test_block_append_accepts_strings_and_nested_objects"
        - "test_block_append_preserves_requested_hierarchy_and_order"
        - "test_block_update_changes_content_and_verifies_readback"
        - "test_block_delete_removes_block_from_followup_reads"
        - "test_invalid_nested_payload_fails_before_first_write"
        - "test_block_append_missing_page_raises_explicit_error"
        - "test_block_update_missing_uuid_raises_explicit_error"
        - "test_block_delete_missing_uuid_raises_explicit_error"
  key_links:
    - from: "tests/test_write.py"
      to: "src/logseq_mcp/tools/write.py"
      via: "imports inside each test body"
      pattern: "from logseq_mcp.tools.write import"
    - from: "tests/test_write.py"
      to: "src/logseq_mcp/server.py"
      via: "AppContext test harness"
      pattern: "AppContext"
---

<objective>
Create the red-state test scaffold for Phase 3 write tools.

Purpose: Wave 1 must exist before implementation starts. These tests define the required write behavior, expected RPC sequencing, and read-after-write verification contract for `page_create`, `block_append`, `block_update`, and `block_delete`.

Output: `tests/test_write.py` with failing but fully specified tests for WRIT-01 through WRIT-05.
</objective>

<execution_context>
@/home/berga/.claude/get-shit-done/workflows/execute-plan.md
@/home/berga/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-write-tools/03-RESEARCH.md
@.planning/phases/03-write-tools/03-VALIDATION.md

<interfaces>
<!-- Match the Phase 2 test style: import tool functions inside the test body -->

Mock context pattern:
```python
from unittest.mock import AsyncMock
from logseq_mcp.server import AppContext

mock_client = AsyncMock()
mock_ctx = AsyncMock()
mock_ctx.request_context.lifespan_context = AppContext(client=mock_client)
```

Write tool return contract:
- Public MCP write tools return JSON strings
- Tests parse with `json.loads(...)`
- Read-after-write verification can be asserted by checking follow-up `client._call(...)` usage or mocked read payloads

Expected implementation module:
```python
from logseq_mcp.tools.write import (
    page_create,
    block_append,
    block_update,
    block_delete,
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tests/test_write.py with Phase 3 red-state tests</name>
  <files>tests/test_write.py</files>
  <behavior>
    - `test_page_create_with_properties_and_initial_blocks` asserts `page_create` calls `logseq.Editor.createPage` with page properties as the second argument and appends initial blocks only after page creation succeeds
    - `test_block_append_accepts_strings_and_nested_objects` asserts `block_append` accepts a list mixing plain strings and nested `{content, properties, children}` objects
    - `test_block_append_preserves_requested_hierarchy_and_order` asserts root blocks use `appendBlockInPage`, child blocks use `insertBlock`, and sibling/child order is preserved
    - `test_block_update_changes_content_and_verifies_readback` asserts `block_update` calls `updateBlock`, then performs a follow-up read check using the Phase 2 read path
    - `test_block_delete_removes_block_from_followup_reads` asserts `block_delete` calls `removeBlock`, then verifies the target UUID is absent on follow-up read
    - `test_invalid_nested_payload_fails_before_first_write` asserts malformed nested input raises an error before any write RPC runs
    - `test_block_append_missing_page_raises_explicit_error` asserts a missing page surfaces an explicit failure
    - `test_block_update_missing_uuid_raises_explicit_error` asserts a missing UUID surfaces an explicit failure
    - `test_block_delete_missing_uuid_raises_explicit_error` asserts a missing UUID surfaces an explicit failure
  </behavior>
  <action>
    Create `tests/test_write.py` with fully specified async tests. Follow the same style as `tests/test_core.py`:

    1. Keep imports minimal at module level: `json`, `pytest`, `AsyncMock`.
    2. Import write tools inside each test body so pytest can collect the full file even before implementation exists.
    3. Add a small helper to flatten block UUIDs or content from readback payloads when asserting hierarchy/order.
    4. Use async fake `_call` functions or `AsyncMock(side_effect=...)` to capture exact method/argument ordering.
    5. Keep all assertions real. Do not use `pass`, `xfail`, or `importorskip`.

    Required test names:
    - `test_page_create_with_properties_and_initial_blocks`
    - `test_block_append_accepts_strings_and_nested_objects`
    - `test_block_append_preserves_requested_hierarchy_and_order`
    - `test_block_update_changes_content_and_verifies_readback`
    - `test_block_delete_removes_block_from_followup_reads`
    - `test_invalid_nested_payload_fails_before_first_write`
    - `test_block_append_missing_page_raises_explicit_error`
    - `test_block_update_missing_uuid_raises_explicit_error`
    - `test_block_delete_missing_uuid_raises_explicit_error`

    Use representative payloads:
    - Page properties like `{"type": "Project", "status": "active"}`
    - Mixed block append payloads like `["root 1", {"content": "root 2", "properties": {"kind": "task"}, "children": [{"content": "child"}]}]`
    - Follow-up read payloads shaped like existing Phase 2 `getPageBlocksTree` / `getBlock` responses
    - Missing-target payloads should assert `McpError` semantics instead of silent falsey returns
  </action>
  <verify>
    <automated>cd /home/berga/Workspace/projects/logseq-mcp && uv run pytest tests/test_write.py -x -q</automated>
  </verify>
  <done>`tests/test_write.py` is collected successfully; the run fails RED due to missing write implementation rather than import-time or syntax-time errors</done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/test_write.py -q`.

Expected: collected tests fail in RED state until `src/logseq_mcp/tools/write.py` exists and write tools are implemented. Any syntax error, collection error, or module-level import error must be fixed before moving to Plan 02.
</verification>

<success_criteria>
- `tests/test_write.py` exists and pytest collects all named tests
- Each Phase 3 requirement has at least one explicit test assertion tied to it
- Negative-path tests exist for missing page and missing UUID cases
- Red-state failure is due to unimplemented write tools, not broken test scaffolding
- Full suite excluding `tests/test_write.py` still runs clean
</success_criteria>

<output>
After completion, create `.planning/phases/03-write-tools/03-01-SUMMARY.md`
</output>
