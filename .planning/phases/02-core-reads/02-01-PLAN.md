---
phase: 02-core-reads
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_core.py
autonomous: true
requirements:
  - READ-01
  - READ-02
  - READ-03
  - READ-04
  - READ-05
  - READ-06

must_haves:
  truths:
    - "tests/test_core.py exists and pytest collects all 6 test functions"
    - "All 6 tests FAIL (RED state) because tools/core.py has no implementation yet"
    - "Running `uv run pytest tests/test_core.py -x -q` exits with a test failure, not an import error or syntax error"
  artifacts:
    - path: "tests/test_core.py"
      provides: "Failing test stubs for READ-01 through READ-06"
      exports: ["test_get_page_no_duplicate_uuids", "test_get_page_nesting_correct", "test_get_page_uses_name", "test_get_block_returns_block", "test_list_pages_namespace_filter", "test_get_references_parses_response"]
  key_links:
    - from: "tests/test_core.py"
      to: "logseq_mcp.tools.core"
      via: "import inside test body"
      pattern: "from logseq_mcp.tools.core import"
---

<objective>
Create the test scaffold for Phase 2 in tests/test_core.py with 6 failing tests (RED state).

Purpose: Nyquist rule — every automated verify command must reference a test that already exists before implementation begins. This plan creates all 6 test stubs covering READ-01 through READ-06. Tests are written in full (not just `pass` stubs) so the executor in Plan 02 and 03 can implement against specific assertions.

Output: tests/test_core.py with 6 async test functions — all failing because the tools are not implemented yet.
</objective>

<execution_context>
@/home/berga/.claude/get-shit-done/workflows/execute-plan.md
@/home/berga/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-reads/02-RESEARCH.md
@.planning/phases/02-core-reads/02-VALIDATION.md

<interfaces>
<!-- Existing mock pattern from tests/test_server.py — replicate exactly -->

Fixture (already in tests/test_server.py, replicate in test_core.py):
```python
@pytest.fixture
def token_env(monkeypatch):
    monkeypatch.setenv("LOGSEQ_API_TOKEN", "test-token")
```

AppContext (from src/logseq_mcp/server.py):
```python
@dataclass
class AppContext:
    client: LogseqClient
```

Mock context pattern:
```python
mock_client = AsyncMock()
mock_client._call = fake_call  # async def fake_call(method, *args)

mock_ctx = AsyncMock()
mock_ctx.request_context.lifespan_context = AppContext(client=mock_client)

result = await some_tool(mock_ctx, ...)
data = json.loads(result)
```

Tool import pattern (import inside test body to get RED on missing impl):
```python
async def test_something(token_env):
    from logseq_mcp.tools.core import get_page  # import at call site
    ...
```
</interfaces>
</context>

<feature>
  <name>test_core.py — failing test stubs for READ-01 through READ-06</name>
  <files>tests/test_core.py</files>
  <behavior>
    - test_get_page_no_duplicate_uuids (READ-01): Build raw block list where UUID "B" appears at top level AND as a child of "A". Call get_page. Collect all UUIDs from result["blocks"] recursively. Assert len(all_uuids) == len(set(all_uuids)).
    - test_get_page_nesting_correct (READ-02): Build raw block list with A->B parent/child. Call get_page. Assert result["blocks"] has 1 top-level block (A), and A has 1 child (B). Assert B does NOT appear at top level.
    - test_get_page_uses_name (READ-03): Capture which args getPageBlocksTree was called with. Assert the first arg is the page name string "my-page", NOT a UUID.
    - test_get_block_returns_block (READ-04): Mock getBlock to return a block dict with uuid="block-uuid", content="Hello". Call get_block(ctx, "block-uuid"). Assert result["uuid"] == "block-uuid" and result["content"] == "Hello".
    - test_list_pages_namespace_filter (READ-05): Mock getAllPages to return 3 pages: "projects/alpha", "projects/beta", "daily". Call list_pages(ctx, namespace="projects"). Assert result has 2 entries, none named "daily".
    - test_get_references_parses_response (READ-06): Mock getPageLinkedReferences to return [[page_dict, [block_dict]]]. Call get_references(ctx, "target-page"). Assert result has 1 entry with keys "page" and "blocks".
  </behavior>
  <implementation>
    Write all tests as fully-specified assertions (not `pass` stubs). Tests WILL fail (RED) because get_page, get_block, list_pages, get_references do not exist yet in tools/core.py. That RED state is the intended output.

    IMPORTANT: The import of each tool must happen inside the test function body (not at module level). This ensures pytest can still collect all tests even if one import fails.

    Helper: add a module-level `_collect_uuids(blocks: list) -> list[str]` function that recursively walks a list of block dicts (from model_dump output) and returns all uuid values found at any depth. Used in test_get_page_no_duplicate_uuids and test_get_page_nesting_correct.

    Do NOT use pytest.importorskip. Do NOT use `pass` or `pytest.fail("not implemented")` — write real assertions against the (future) real output.

    Use the token_env fixture (monkeypatches LOGSEQ_API_TOKEN=test-token) as first arg in all test functions.

    pytest-asyncio asyncio_mode is already "auto" in pyproject.toml — no `@pytest.mark.asyncio` decorator needed.
  </implementation>
</feature>

<verification>
<automated>uv run pytest tests/test_core.py -q 2>&1 | tail -5</automated>

Expected: 6 FAILED, 0 errors. Import errors or syntax errors indicate a problem with the test file itself — fix before proceeding to Plan 02.
</verification>

<success_criteria>
- tests/test_core.py exists with 6 async test functions
- `uv run pytest tests/test_core.py -q` collects 6 tests and all 6 FAIL (not error)
- No syntax errors, no import errors at module level
- Full test suite still passes: `uv run pytest tests/ -x -q --ignore=tests/test_core.py` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-reads/02-01-SUMMARY.md`
</output>
